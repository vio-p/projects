#pragma once

/**
 * @brief This enum contains the 3 predefined strategies used by the computer player when picking a position.
 */

enum class EStrategyType
{
	Easy, /**< Easy strategy. */
	Medium, /**< Medium strategy. */
	Hard /**< Hard strategy. */
};
